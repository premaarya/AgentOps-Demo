- 2026-03-07: The active application root is the repository root. agents/, gateway/, mcp-servers/, ui/, docs/, and the other runtime folders now live directly under the repo.
- 2026-03-07: Main quality gates for the app are npm test, npm run typecheck, npm run build, and npm run lint from the repository root.
- 2026-03-07: npm run lint now passes (exit 0) with biome.json config in place. 25 warnings (noForEach, noExplicitAny) are acceptable; 0 errors.
- 2026-03-07: The primary runtime UI is the static app under ui/, served by the gateway at http://localhost:8000.
- 2026-03-07: **Vanilla JS UI is the single source of truth**: All UI functionality lives in ui/ folder (workflow-designer.js, app.js, styles.css). No React duplication needed. Focus enhancements on existing proven vanilla JS implementation.
- 2026-03-10: Runtime workflow design behavior must be implemented in ui/ first. The dashboard/ folder exists in the repo but is incomplete and should be treated as a future alignment target, not the active source of workflow UX.
- 2026-03-10: Workflow orchestration semantics are modeled per agent with kind/stage/lane. The workflow type alone is not enough to represent orchestrator -> parallel -> sequential execution.
- 2026-03-10: Design-tab validation is the correct home for workflow structure rules. Save/push should block only on design errors, while the second tab should focus on scenario testing rather than tool registry browsing.
- 2026-03-10: Deployment guidance for the runnable app belongs under docs/. Use the repository root for local setup and azd commands.
- 2026-03-10: Treat the TypeScript gateway as the source of truth for workflow definitions and active workflow versions. Treat the Python MAF executor as the source of truth for in-flight execution state.
- 2026-03-10: Declarative runtime assets for the contract demo belong under prompts/, config/agents, config/workflows, config/schemas, and policy/evaluation data locations. Runtime code should consume these assets rather than infer them from hardcoded workflow graphs.
- 2026-03-10: The active workflow contract is materialized as data/runtime/active-workflow.json plus immutable package snapshots under data/runtime/packages/. Gateway routes should read/write through gateway/src/services/workflowRegistry.ts rather than keeping workflow state in memory.
- 2026-03-10: Agent YAML configs under config/agents/ must reference real prompts, templates, examples, and JSON schemas. Python MAF loaders should resolve these repo-relative asset paths via agents/microsoft-framework/config.py helpers.
- 2026-03-10: Prompt files, few-shot examples, templates, and evaluation/drift artifacts must align with the declarative JSON schemas under config/schemas/. Do not let prompts teach legacy output shapes that differ from the declared schema.
- 2026-03-10: DS documentation for the runnable app belongs under docs/data-science/ and should reflect the latest corpus-matched evaluation and drift evidence rather than legacy demo placeholders.
- 2026-03-10: In the Python MAF demo scripts, prefer a single specific catch for known recoverable errors and let unexpected exceptions propagate; secondary "unexpected" catch blocks tend to trigger the repo's Python analyzer as overly broad or redundant.
- 2026-03-10: For Azure-hosted live deployments in the contract demo, treat `FOUNDRY_PROJECT_ENDPOINT` as optional and fall back to `FOUNDRY_ENDPOINT`. App Service settings should be populated from IaC outputs, not from repo secrets.
- 2026-03-10: The `azure.yaml` postdeploy hook is a hard gate. In live mode it must send `x-admin-key` using `DEPLOY_ADMIN_KEY` and fail the deployment on any non-2xx registration response.
- 2026-03-11: Live deployment verification for the contract demo must send `x-admin-key` to `/api/v1/deploy/status` and `/api/v1/deploy/pipeline`. Reuse `scripts/deploy/verify-deployment.ps1` with `-DeployAdminKey` instead of duplicating curl logic in workflows.
- 2026-03-11: The ACA deployment path is rooted at the repository workflow layer, not `azure.yaml`. Provision ACA base resources from `infra/container-apps.bicep`, build images with `az acr build`, then create or update the container app from the workflow.
- 2026-03-11: For ACA live runtime, set `FOUNDRY_AUTH_MODE=managed-identity` and `FOUNDRY_MANAGED_IDENTITY_CLIENT_ID`; do not inject `FOUNDRY_API_KEY` into the container app.
- 2026-03-11: For App Service live runtime, set `FOUNDRY_AUTH_MODE=managed-identity` from IaC, rely on the App Service system-assigned identity, and keep `FOUNDRY_PROJECT_ENDPOINT` aligned to `FOUNDRY_ENDPOINT`.
- 2026-03-11: Treat `config/stages/contract-lifecycle.json` as the declarative source for business-stage metadata. Workflow packages should reference that asset in `manifest_references` and derive active stage-to-agent mappings from the ordered runtime agent bindings instead of hardcoding UI-only stage lists elsewhere.
- 2026-03-11: For the active vanilla JS dashboard, normalize workflow payloads in `ui/app.js` before rendering. The UI must handle both local designer workflows and gateway `active/package` payloads instead of assuming a single shape.
- 2026-03-11: When a backend endpoint still reports agent-level telemetry, keep the gateway contract stable and aggregate to contract stages in the UI from `getWorkflowStagesForTesting(...)` unless a broader runtime contract change is explicitly needed.
- 2026-03-11: For live workflow narration in the static dashboard, use a shared role-to-stage lookup derived from `getWorkflowStagesForTesting(...)` and expose it to `ui/api.js` via `window` helpers. Do not hardcode Intake/Extraction/Compliance/Approval labels in simulated or real-mode logs.
- 2026-03-11: When finishing a stage-first migration, preserve old gateway response fields for compatibility and add the stage-native payload alongside them. The UI can then prefer `contract_stages` while older consumers continue using `agents`.
- 2026-03-12: For contract-agentops-demo, treat the active user-facing workflow as the 6-stage pre-execution lifecycle: Intake, Drafting, Internal Review, Compliance, Negotiation, Approval. Keep post-approval roles and `extraction` support only as compatibility or future-extension assets, not as the default runtime baseline.
- 2026-03-12: The repository is now flattened. Do not reintroduce `contract-agentops-demo/` path prefixes in workflows, docs, or scripts; use repository-root paths instead.
