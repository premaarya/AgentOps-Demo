- 2026-03-07: Review scope centered on the runnable workspace, which now lives at the repository root, then checked duplication only where it affected wiring or drift.
- 2026-03-07: Archived the React dashboard from runtime by removing gateway/startup wiring while keeping the folder in the repo for reference.
- 2026-03-07: Removed dashboard from npm workspaces array; dashboardPort removed from gateway config; CORS simplified to gateway origin only.
- 2026-03-07: Created biome.json excluding dashboard/, docs/ux/prototypes/, with noForEach/noExplicitAny/noNonNullAssertion demoted to warnings.
- 2026-03-07: **DECISION REVERSAL - Removed all React implementation**: After implementing Interactive Workflow Designer in React, recognized that vanilla JS UI in ui/ folder already provides perfect working solution. Removed entire dashboard/ directory and React-specific documentation to avoid code duplication and maintain single source of truth. Focus on enhancing existing vanilla JS workflow-designer.js instead.
- 2026-03-10: Fixed workflow-type design limits by introducing stage-aware agent metadata (kind/stage/lane) in the active ui/ designer and extending gateway/dashboard schemas for compatibility. This preserves current visuals while enabling orchestrator, parallel, and merge patterns.
- 2026-03-10: The dashboard/ React code should not be treated as removed; it exists but is structurally incomplete. Use it only for schema alignment until missing files are restored.
- 2026-03-10: Repurposed the second UI tab from Build to Test. Structural workflow validation now belongs to Design and is enforced during authoring/save/push; the second tab is reserved for scenario-based workflow testing.
- 2026-03-10: Added docs/SETUP-DEPLOYMENT.md as the repo-specific runbook for local setup and Azure App Service deployment via azd.
- 2026-03-10: Moved the executable GitHub Actions workflows to the repository root and added a manual Azure deployment workflow with post-deploy verification for the contract demo.
- 2026-03-10: Consolidated contract architecture guidance into a single canonical ADR at docs/adr/ADR-ContractAgentOps-Demo.md. Removed the temporary alignment ADR/spec and the contract spec file to avoid conflicting architecture guidance. Foundry plus Microsoft Agent Framework is now the sole documented direction for the contract demo.
- 2026-03-10: Consolidated product requirements into a single canonical PRD at docs/prd/PRD-ContractAgentOps-Demo.md. Merged the Interactive Workflow Designer scope into the main PRD and deleted the duplicate PRD to avoid conflicting product requirements.
- 2026-03-10: Removed the remaining duplicate workflow-designer implementation docs at docs/adr/ADR-InteractiveWorkflowDesigner.md and docs/specs/SPEC-InteractiveWorkflowDesigner.md. The contract demo now keeps one canonical ADR and one canonical PRD only.
- 2026-03-10: Resolved the runtime operating model for the contract demo as mixed-runtime, not Node-only. TypeScript remains the control plane for the static UI, gateway, and MCP servers, while Python is the primary Microsoft Agent Framework execution plane.
- 2026-03-10: Added the canonical technical specification at docs/specs/SPEC-ContractAgentOps-Demo.md. It defines the gateway-to-MAF runtime contract, workflow package ownership, execution-state ownership, and the required declarative asset set.
- 2026-03-10: Implemented the gateway-side workflow registry as a persistent JSON-backed control-plane service that materializes immutable active workflow packages under data/runtime/. The gateway is now responsible for workflow definition storage and active package activation.
- 2026-03-10: Switched Azure deployment automation to Foundry-first provisioning. The deploy workflow now provisions the Azure OpenAI account through Bicep, generates a per-run DEPLOY_ADMIN_KEY, and creates the model deployment only when it does not already exist.
- 2026-03-10: Moved model deployment ownership out of gateway runtime auto-provisioning and into the Azure deployment workflow. Gateway registration now verifies the configured deployment and fails fast if the deployment is missing.
- 2026-03-11: Added Azure Container Apps as an optional deployment lane for the contract demo instead of replacing App Service. App Service remains the default azd path; ACA uses image-based deployment through ACR plus a dedicated GitHub Actions workflow.
- 2026-03-11: Hardened the ACA lane to use managed identity for Foundry runtime access and made the ACA workflow manual-only. App Service remains the default automatic deployment path.
- 2026-03-11: Moved the App Service lane to managed identity for Foundry access as well. App Service now uses a system-assigned identity plus `Cognitive Services OpenAI User` RBAC so both Azure hosting lanes share the same runtime auth posture and no App Service Foundry key is emitted from Bicep.
- 2026-03-11: Added a separate product PRD for the broader AI contract lifecycle workflow at `docs/prd/PRD-AI-Contract-Lifecycle-Workflow.md` instead of changing the app's canonical demo PRD. This avoids conflicting with the runnable demo scope while capturing a product-level workflow expansion.
- 2026-03-11: Explicitly separated Contract Lifecycle from AgentOps in product planning. Contract Lifecycle is the business workflow for agreements; AgentOps remains the SDLC and operational lifecycle for AI design, deployment, monitoring, evaluation, drift, and feedback.
- 2026-03-11: Updated the canonical ADR and spec for the contract demo to formalize a dual-lifecycle architecture. Business-stage state stays separate from AgentOps state, and the active workflow package must carry an explicit stage-to-agent mapping catalog.
- 2026-03-11: Implemented the first runtime slice of the dual-lifecycle model in the contract demo. The gateway workflow package now publishes a declarative contract stage catalog plus a derived stage-to-agent map, and the workflow routes expose both the active stage map and the catalog for UI and runtime consumers.
- 2026-03-11: Wired the active static dashboard in ui/ to the gateway workflow package. Test, Deploy, and Live now prefer `contract_stage_map` and fall back to legacy agent-stage inference only when no active package is available.
- 2026-03-11: Kept the monitor backend schema stable and composed contract-stage observability in the UI. The monitor panel now derives stage-level traces, latency, token, and audit views from the active workflow's `contract_stage_map` plus the existing agent-level monitor payload.
- 2026-03-11: Removed the remaining legacy four-agent narration from the live workflow runtime UI. Simulated and real-mode activity logs now resolve labels from the active contract stage map, while execution behavior stays unchanged.
- 2026-03-11: Completed the remaining runtime gap by adding native contract-stage telemetry to `GET /api/v1/monitor/:contractId`. The gateway now emits both legacy `agents` telemetry and stage-first `contract_stages`, and the dashboard prefers the backend stage payload when available.
- 2026-03-12: Expanded the executable workflow from the 4-agent MVP to dedicated lifecycle-aligned agents, then narrowed the active default runtime to the stronger 6-stage pre-execution baseline. The primary runtime package now ends at Approval Routing, while signature, obligations, renewal, analytics, and legacy `extraction` support remain available only as compatibility or future-extension assets.
- 2026-03-12: Flattened the app from `contract-agentops-demo/` into the repository root, restored all runtime directories under root, and updated workflows, docs, and memory references to the root-based layout.
- 2026-03-16: Expanded intake classification to support Services, Sales, Distribution, Supply, License, SaaS/Cloud, Promissory Note, and Loan agreement families. Kept the gateway runtime contract stable by normalizing schema-shaped intake responses back into the legacy `type` and `confidence` fields at the agent boundary.
