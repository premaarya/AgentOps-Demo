- 2026-03-10: Implemented review follow-up items 1 and 4 for the contract demo.
- Hardened Python exception handling in agents/microsoft-framework/agents.py, workflows.py, demo.py, setup.py, and __init__.py by removing broad or redundant catch blocks and keeping only specific recoverable catches.
- Expanded intake taxonomy to include Consortium and Partnership in prompts/intake-system.md.
- Updated regression coverage in tests/ai-assets.test.ts and aligned related docs in README.md and docs/data-science/MODEL-CARD-ContractAgentOps-Demo.md.
- Validation: npm test passes (63/63). Remaining setup.py diagnostics are pre-existing lazy-logging and subprocess check issues, not exception-handling issues.
- 2026-03-07: Static UI path is now the working runtime. Verified: npm start boots successfully on Windows, GET /api/v1/health is healthy, GET /api/v1/tools returns all 8 MCP servers, npm test passes, and standalone scripts test-comprehensive.ts plus test-deployment.ts pass.
- 2026-03-07: Cleanup complete — dashboard retired from workspaces, dashboardPort removed, CORS cleaned, biome.json created, all formatting auto-fixed, docs updated with archival notices. Tests 49/49, lint 0 errors. Ready for next phase.
- 2026-03-07: **REMOVED ALL REACT IMPLEMENTATION**: Deleted entire dashboard/ directory and React-related documentation. Kept only the working vanilla JS UI in ui/ folder. Focused on single, proven implementation that already works perfectly (workflow-designer.js with full CRUD operations, drag-drop, save/load, push to pipeline). Commit 00c1bd5.
- 2026-03-10: Reviewed workflow designer architecture after user reported parallel workflows do not behave as designed. Root cause was a flat workflow model that only stored type plus agents/order, making orchestrator + parallel + sequential layouts impossible to represent.
- 2026-03-10: Implemented the fix in the active ui/ workflow designer without changing the visual language. Added agent kind/stage/lane metadata, stage-based rendering in ui/workflow-designer.js and ui/app.js, supporting CSS in ui/styles.css, and schema compatibility in gateway/src/routes/workflows.ts plus dashboard/src/context/WorkflowContext.tsx.
- 2026-03-10: Validation passed after the fix: node --check for ui/workflow-designer.js and ui/app.js succeeded, and npm run build succeeded across gateway, MCP servers, and agents.
- 2026-03-10: Remaining gap is manual browser smoke validation of the staged designer behavior. React dashboard files remain incomplete, so the runtime fix stays in ui/ for now.
- 2026-03-10: Repurposed the Build tab into a workflow Test tab with scenario-based checks. Added authoring-time workflow validation to the Design tab, surfaced validation findings in the inventory area, and blocked invalid save/push actions. UI scripts and workspace build both pass after the refactor.
- 2026-03-10: Added a setup and deployment runbook at docs/SETUP-DEPLOYMENT.md and linked it from README. The guide documents the current Azure live-mode caveat around missing FOUNDRY_PROJECT_ENDPOINT app settings in App Service.
- 2026-03-10: Closed the live deployment gap by wiring FOUNDRY_PROJECT_ENDPOINT through Bicep/App Service, moved GitHub workflows to the repository root, added a manual Azure deployment workflow, and added post-deploy verification script/docs.
- 2026-03-10: Replaced the contract baseline ADR contents with the Foundry plus Microsoft Agent Framework decision and deleted the temporary alignment ADR plus both contract spec files. The single source of truth is now docs/adr/ADR-ContractAgentOps-Demo.md. No runtime code changed in this step.
- 2026-03-10: Replaced the two contract PRDs with one canonical PRD by merging the Interactive Workflow Designer scope into docs/prd/PRD-ContractAgentOps-Demo.md and deleting the duplicate PRD file. Also fixed the remaining stale review reference to the deleted PRD.
- 2026-03-10: Deleted the standalone workflow-designer ADR and spec. The contract demo docs now keep only the canonical contract ADR plus canonical contract PRD, with no remaining duplicate design-doc pair.
- 2026-03-10: Added the new canonical spec at docs/specs/SPEC-ContractAgentOps-Demo.md. The spec resolves the Node-only versus mixed-runtime question in favor of a TypeScript control plane plus Python MAF executor, defines the gateway-to-executor runtime contract, and establishes the declarative asset inventory as the source of truth.
- 2026-03-10: Implemented the first architecture-aligned runtime slice. Added gateway/src/services/workflowRegistry.ts, rewired workflow routes to persistent definitions plus active package materialization, added declarative agent/schema/template/example assets, and added Python asset resolution plus YAML agent loading. Focused Vitest validation passed for workflow-registry.test.ts, config.test.ts, and pipeline.test.ts.
- 2026-03-10: Completed the deployment automation follow-up requested after the readiness review. Added automatic deploy triggers from `main` and `v*` tags, provisioned Azure OpenAI via Bicep, generated and wired `DEPLOY_ADMIN_KEY`, made the `azd` postdeploy registration hook fail fast, and changed gateway live config so `FOUNDRY_PROJECT_ENDPOINT` falls back to `FOUNDRY_ENDPOINT`.
- 2026-03-10: Validation after the deployment automation changes: `npm test` passes (63/63). Remaining GitHub workflow secret diagnostics are static editor warnings on secret names, not runtime or TypeScript failures.
- 2026-03-11: Added an optional Azure Container Apps deployment lane. New assets include `Dockerfile`, `.dockerignore`, `infra/container-apps.bicep`, `.github/workflows/contract-agentops-deploy-aca.yml`, and ACA deployment docs.
- 2026-03-11: Updated deployment verification so live mode can pass `DEPLOY_ADMIN_KEY` through `scripts/deploy/verify-deployment.ps1`. Also updated the existing App Service workflow to use the same admin-key-aware verification path.
- 2026-03-11: Validation after ACA additions: `npm test`, `npm run typecheck`, and `npm run build` pass. `npm run lint` still reports pre-existing formatter issues in generated and data files; no new lint failures were introduced by the ACA changes.
- 2026-03-11: Refactored the gateway live-mode auth path to support `FOUNDRY_AUTH_MODE=api-key|managed-identity`, added `@azure/identity`, switched ACA runtime auth to managed identity, added OpenAI RBAC for the ACA user-assigned identity, and changed the ACA workflow to manual-only. Validation: `npm run typecheck`, `npm test`, and `npm run build` pass; `npm run lint` still reports pre-existing formatter/lint noise outside the touched files.
- 2026-03-11: Moved the App Service lane to managed identity by enabling a system-assigned identity in `infra/modules/app-service.bicep`, removing `FOUNDRY_API_KEY` from App Service settings, and adding `Cognitive Services OpenAI User` RBAC in `infra/main.bicep`. Updated Azure deployment docs to note the new shared auth posture and the required role-assignment permissions for the deploying identity. Validation: Bicep diagnostics report no errors on the touched templates. `az bicep build` could not be run here because the Azure CLI is not installed in this environment.
- 2026-03-11: Created a new product PRD at `docs/prd/PRD-AI-Contract-Lifecycle-Workflow.md` for a 10-stage AI-driven contract lifecycle workflow covering intake, drafting, review, compliance, negotiation, approval, signature, obligations, renewal, and analytics. The PRD includes external market research, AI governance requirements, lifecycle user stories, and compliance considerations without altering the app's canonical demo PRD.
- 2026-03-11: Updated the product-level contract lifecycle PRD to clarify that Contract Lifecycle is the business workflow and AgentOps remains the separate SDLC/operational lifecycle. Added `docs/prd/BACKLOG-AI-Contract-Lifecycle-Workflow.md` with feature and story decomposition, including a dedicated feature for correlating business workflow outcomes with AgentOps evidence while keeping both lifecycle models separate.
- 2026-03-11: Updated the canonical app PRD at `docs/prd/PRD-ContractAgentOps-Demo.md` so the demo now explicitly frames Contract Lifecycle as the business workflow and AgentOps as the AI SDLC/operational lifecycle. Added dual-lifecycle wording to goals, requirements, user-flow narrative, constraints, and appendix.
- 2026-03-11: Added a new cross-cutting planning requirement that contract lifecycle stages should be representable by one or more agents so they fit naturally into AgentOps design, test, deploy, run, monitor, evaluate, detect, and feedback flows. Updated the top-level PRD, the backlog, and the canonical demo PRD with an agentized stage model while preserving separate business-stage and AgentOps-stage state models.
- 2026-03-11: Updated `docs/adr/ADR-ContractAgentOps-Demo.md` and `docs/specs/SPEC-ContractAgentOps-Demo.md` to encode the same model architecturally. Added a formal contract-stage catalog, stage-to-agent mapping requirements, lifecycle-separation rules, and a concrete recommended mapping for all 10 contract stages.
- 2026-03-11: Completely overhauled `docs/ux/UX-ContractAgentOps-Dashboard.md`. Replaced the legacy flat four-agent design narrative with the contract-stage-first model. Realigned the UI specifications to properly separate the Contract Lifecycle (business workflow) from the AgentOps lifecycle (AI visibility/management), updating all views, metrics, system colors, and animations.
- 2026-03-11: Implemented the gateway-side contract stage catalog and active stage map. Added `config/stages/contract-lifecycle.json`, extended `buildWorkflowPackage()` to emit `contract_stage_map`, exposed `/api/v1/workflows/active/stage-map` and `/api/v1/workflows/stages/catalog`, and updated the workflow package schema and tests.
- 2026-03-11: Validation for the new contract requirement slice: `npm test -- workflow-registry.test.ts`, `npm run typecheck`, full `npm test`, and `npm run build` all pass from the repository root.
- 2026-03-11: Completed the next slice in the active static dashboard. `ui/app.js` now normalizes active workflows, hydrates from `/api/v1/workflows/active/package`, and renders Test/Deploy/Live from `contract_stage_map` when present. `ui/index.html` and `ui/styles.css` were updated to remove the default legacy four-agent live/deploy placeholders. Validation: editor diagnostics clean, `npm test` passes (65/65), and `npm run build` passes.
- 2026-03-11: Completed the monitor-panel follow-up in the active static dashboard. `ui/app.js` now composes stage-level monitor views from the active workflow plus optional real monitor payloads, `ui/api.js` routes real monitor rendering through the shared stage-first renderer, and `ui/index.html` / `ui/styles.css` now use contract-stage-first monitor placeholders and labels. Validation: editor diagnostics clean, `npm test` passes (65/65), and `npm run build` passes.
- 2026-03-11: Completed the live-runtime wording cleanup in the active static dashboard. `ui/app.js` now resolves stage context for simulated execution and safe log rendering, and `ui/api.js` uses the same stage context for WebSocket events and HITL outcomes in real mode. Validation: editor diagnostics clean, `npm test` passes (65/65), and `npm run build` passes.
- 2026-03-11: Completed the native stage-telemetry backend slice. `gateway/src/routes/audit.ts` now returns `stage_map_reference` and `contract_stages` in `/api/v1/monitor/:contractId`, `ui/app.js` prefers the backend stage payload, and `tests/audit-routes.test.ts` covers the new response contract. Validation: editor diagnostics clean, `npm test` passes (66/66), and `npm run build` passes.
- 2026-03-12: Reduced the active runnable demo from the earlier 10-stage lifecycle to the agreed 6-stage pre-execution baseline: Intake, Drafting, Internal Review, Compliance, Negotiation, Approval. Updated the contract stage catalog, gateway stage mapping, deploy summary expectations, default workflow seed, active runtime package, and canonical PRD/ADR/Spec/UX docs. Validation: `npm test` passes (67/67) and VS Code diagnostics report no workspace errors. I reactivated `wf-de80c904` so the active workflow package now stops at Approval Routing.
- 2026-03-12: Flattened the repository by moving the app contents from `contract-agentops-demo/` to the repo root, restored the product and review docs under root `docs/`, updated workflow and documentation path references, ran `npm install`, and revalidated with `npm test`, `npm run typecheck`, and `npm run build` from the repository root.
- 2026-03-12: Fixed the startup launcher false-negative issue by extracting health polling into `startup/health.ts`, increasing startup tolerance, and stopping early only when child processes exit. Added `tests/startup-health.test.ts`. Validation: targeted startup tests pass, full `npm test` passes (70/70), `npm run typecheck` passes, `npm run build` passes, and the live local health check confirms the UI stack is online at `http://localhost:8000`.